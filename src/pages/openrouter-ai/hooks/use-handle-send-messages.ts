import {
  useCallback
} from 'react'

import {
  IParamsFetch
} from '@/data'

export default function useHandleSendMessages(): (params: IParamsFetch[]) => void {
  return useCallback(() => {
    
  }, [])
}